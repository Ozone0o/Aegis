# Changelog

## Unreleased

- Complete the canonical Aegis package migration across source, tests, examples,
  configuration, ROS metadata, and the `aegis` entry point.
- Require argument-list recovery commands by default and make shell execution an
  explicit `unsafe_shell` deployment decision.
